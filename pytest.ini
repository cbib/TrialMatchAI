[pytest]
testpaths = tests
addopts = -q
markers =
    integration: tests that require external services or large models
    e2e: synthetic public CLI end-to-end tests with a real local search database
